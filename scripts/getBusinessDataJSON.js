const csv = require('csvtojson');
const fs = require('fs');
const moment = require('moment');
const { deflateSync } = require('zlib');

(async () => {
    const byDays = {};
    const byWeeks = {};
    const byMonths = {};
    const byComplexId = {
      days: {},
      weeks: {},
      months: {},
    };

    await Promise.all()


})();